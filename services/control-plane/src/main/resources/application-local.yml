spring:
  config:
    activate:
      on-profile: local

logging:
  structured:
    format:
      console:
  pattern:
    console: "%d{yyyy-MM-dd'T'HH:mm:ss.SSSXXX} %-5level [%thread] [%X{correlationId:-none}] %logger{36} - %msg%n"

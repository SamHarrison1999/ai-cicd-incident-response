# Phase 12 operational-learning workspace

The workspace presents bounded trend projections and adjacent-window comparisons for an explicitly selected organisation and project.

It supports loading, dimension and key filters, trend selection, comparison display, suppression state, observation windows, aggregation version, and source references. Empty and suppressed results are presented as non-actionable states. No workspace action writes to incidents, providers, policy, or production systems.

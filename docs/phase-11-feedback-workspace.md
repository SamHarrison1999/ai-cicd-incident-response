# Phase 11 Batch 4: feedback workspace

The feedback workspace presents tenant-scoped aggregate outcomes from the feedback API. Operators can select an organisation and project, filter by policy version and date, and inspect sample counts, outcome counts, and suppression reasons.

The workspace is deliberately read-only. It has no retraining, provider configuration, policy mutation, incident remediation, or raw-comment display controls.
